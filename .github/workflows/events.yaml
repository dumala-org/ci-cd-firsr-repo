name: Repositoryu events
on: 
  push:
  pull_request: [opened,assigned,reopened,synchronize]
  issues: [opened]
jobs: 
  checkout-test:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo $GITHUB_REF
          echo $GITHUB_SHA
      - uses: actions/checkout@v3